## Chạy đoạn này vào terminal

pip install ffmpeg
pip install pydub librosa soundfile
pip install --upgrade librosa
pip install gTTS SpeechRecognition
pip install googletrans==4.0.0-rc1
pip install streamlit streamlit-audiorec
